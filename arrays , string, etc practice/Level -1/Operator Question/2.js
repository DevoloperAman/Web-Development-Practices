// 2. Create a function that takes two numbers and returns `true` if the first is
// greater than the second, using the `>` operator.
//  - Explanation : Teaches basic comparison operations.

let check = (a,b)=> { 
        if(a>b){
            return a
        }

        else{
             console.log("Second numebr is biggerr ");
             
        }
}

console.log(check(10,5));
